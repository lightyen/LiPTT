<Page
    x:Class="LiPTT.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LiPTT"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Text="還在施工中..." Margin="10,10,10,60"/>
            <CheckBox Content="安全連線" FontSize="28" Margin="100,10,10,50" VerticalContentAlignment="Center" VerticalAlignment="Center" IsChecked="{Binding ConnectionSecurity, Source={StaticResource GlobalProperty}, Mode=TwoWay}" />
            <Slider Header="圖片留白寬度(%)" FontSize="28" Width="500" Margin="100,10,10,50" HorizontalAlignment="Left" Minimum="0" Maximum="100" Value="{Binding Space, Source={StaticResource GlobalProperty}, Mode=TwoWay, Converter={StaticResource SpaceConverter}}" />
        </StackPanel>
    </Grid>
</Page>
